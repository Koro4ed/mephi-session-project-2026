  693  lsblk
  694  sudo fdisk -l /dev/sdb
  695  sudo parted /dev/sdb mklabel gpt
  696  sudo parted /dev/sdb mkpart primary ext4 0% 100%
  697  sudo mkfs.ext4 -L MEPHI_DATA /dev/sdb1
  698  sudo mkdir -p /data/mephi-web
  699  echo "LABEL=MEPHI_DATA /data/mephi-web ext4 defaults 0 0" | sudo tee -a /etc/fstab
  700  sudo mount -a
  701  df -h /data/mephi-web
  702  sudo systemctl start nginx
  703  sudo systemctl enable nginx
  704  sudo systemctl status nginx
  705  sudo journalctl -u nginx --since "5 minutes ago" > /tmp/nginx_recent_logs.txt
  706  cat /tmp/nginx_recent_logs.txt
  707  sudo groupadd mephi-devs
  708  sudo useradd -m -G mephi-devs mephi-admin
  709  echo "P@ssw0rd2026" | sudo passwd --stdin mephi-admin
  710  sudo chown -R mephi-admin:mephi-devs /data/mephi-web
  711  sudo chmod 2775 /data/mephi-web
  712  ls -ld /data/mephi-web
  713  getenforce
  714  sudo setenforce 1
  715  sudo sed -i 's/SELINUX=.*/SELINUX=enforcing/' /etc/selinux/config
  716  sudo semanage fcontext -a -t httpd_sys_content_t "/data/mephi-web(/.*)?"
  717  sudo restorecon -Rv /data/mephi-web
  718  ls -Z /data/mephi-web
  719  sudo passwd mephi-admin
  720  sudo chown -R mephi-admin:mephi-devs /data/mephi-web
  721  sudo chmod 2775 /data/mephi-web
  722  ls -ld /data/mephi-web
  723  ls -l /usr/sbin/tcpdump
  724  which tcpdump
  725  sudo find / -name tcpdump 2>/dev/null
  726  sudo dnf install -y tcpdump
  727  sudo apt update
  728  sudo apt install -y tcpdump
  729  ls -l /usr/sbin/tcpdump
  730  sudo chmod u-s /usr/sbin/tcpdump
  731  sudo setcap cap_net_raw,cap_net_admin+ep /usr/sbin/tcpdump
  732  getcap /usr/sbin/tcpdump
  733  sudo -u mephi-admin tcpdump --help
  734  sudo apt install -y libcap-ng-utils
  735  ls -l /usr/sbin/tcpdump
  736  which tcpdump
  737  ls -la /usr/bin/tcpdump
  738  sudo apt install -y tcpdump
  739  ls -l /usr/bin/tcpdump
  740  sudo chmod u-s /usr/bin/tcpdump
  741  sudo setcap cap_net_raw,cap_net_admin+ep /usr/bin/tcpdump
  742  getcap /usr/bin/tcpdump
  743  sudo -u mephi-admin tcpdump --help 2>&1 | head -5
  744  echo "root" | sudo tee /etc/ssh/denied_users
  745  echo "root" | sudo tee /etc/ssh/denied_users
  746  sudo tee -a /etc/pam.d/sshd << 'EOF'
  747  sudo sed -i 's/^#PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
  748  sudo sed -i 's/^PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
  749  sudo systemctl restart sshd
  750  sudo apt install -y openssh-server
  751  ls -la /etc/ssh/
  752  sudo sed -i 's/^#PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
  753  sudo sed -i 's/^PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
  754  sudo systemctl restart ssh
  755  sudo -u mephi-admin sh -c 'echo "Hello from Student: 12345" | sudo tee /data/mephi-web/index.html'
  756  sudo usermod -aG sudo mephi-admin
  757  sudo -u mephi-admin sh -c 'echo "Hello from Student: 12345" | sudo tee /data/mephi-web/index.html'
  758  sudo nginx -t
  759  sudo systemctl reload nginx
  760  curl http://localhost
  761  ip addr show
  762  cd ~
  763  history > ~/project_history.txt
